Add verification to all fields to ensure data integrity
Remove additional buyer if all fields are empty
Add persisted state
If buyer >= 2nd and doesn't contain needed information remove from state? or make the fields required?
Complete remaining buyers fields
add switch for seller or agent
add call to database
Fix multi buyers validation for multiple dynamic emails
Add auto complete for Address, state, zipcode, city
Add improved validation for address, state, zipcode, city
Add improved validation for mls
Add improved validation for deed reference number
Change all address fields to State, City, ZipCode
Merge all deadline fields into single block
Validate deadlines against each other
Change Date Built to Year built
Figure out how to properly handle YUP validation with dynamic property names
Fixed Date Field Label when data already exist in field on back press
Limit number of buyers in general page